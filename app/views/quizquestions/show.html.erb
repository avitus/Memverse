<% page_title @quiz_name %>
<div class="white-box-with-margins">

	<h1>Question <%= @question.question_no %> for <%= link_to @quiz.name, quiz_path(@quiz) %></h1>

	<div class="quiz-question">

		<% if @question.question_type == "mcq" %>

			<h3><%= @question.mc_question %></h3>

			<p class="<%= @question.mc_answer == 'A' %>"> A) <%= @question.mc_option_a %> </p>
			<p class="<%= @question.mc_answer == 'B' %>"> B) <%= @question.mc_option_b %> </p>
			<p class="<%= @question.mc_answer == 'C' %>"> C) <%= @question.mc_option_c %> </p>
			<p class="<%= @question.mc_answer == 'D' %>"> D) <%= @question.mc_option_d %> </p>

			Correct: <%= @question.mc_answer %>

		<% else %>

			<%= @question.question_type %>: <%= @question.passage %>

		<% end %>

	</div>

	<br />

	<div>
		<% if current_user == @question.quiz.user %>
			<%= link_to 'Edit', edit_quizquestion_path(@question) %> |
		<% end %>

		<%= link_to 'Back', quiz_path(@quiz) %>
	</div>

</div>
