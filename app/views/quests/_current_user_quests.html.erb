<div>
	<h2>Current Level Goals</h2>
	
	<% for quest in current_user_quests %>
		<% if quest.url %>
			<p>- <%= link_to_if(quest.url, quest.task, quest.url) %></p>
		<% else %>
			<p>- <%= quest.task %></p>
		<% end %>
	<% end %>
	
</div>

