<div class="complete-quiz-question">

    <!-- Quiz Question -->
    <div class="quiz-question">

        <p><%= quiz_question.mc_question %></hp>

    </div>

    <div class="quiz-question-answers">

        <p class="<%= quiz_question.mc_answer == 'A' %>"> A) <%= quiz_question.mc_option_a %> </p>
        <p class="<%= quiz_question.mc_answer == 'B' %>"> B) <%= quiz_question.mc_option_b %> </p>
        <p class="<%= quiz_question.mc_answer == 'C' %>"> C) <%= quiz_question.mc_option_c %> </p>
        <p class="<%= quiz_question.mc_answer == 'D' %>"> D) <%= quiz_question.mc_option_d %> </p>

    </div>

    <div class="quiz-question-stats">
        <table>
            <thead></thead>
            <tbody>
                <tr><th>Category:       </th> <td><%= quiz_question.mcq_category                  %></td></tr>
                <tr><th>Supporting Ref: </th> <td class="quiz-question-supporting-ref"><%= quiz_question.supporting_ref.try(:long_ref) %></td></tr>
                <tr><th>Submitted By:   </th> <td><%= quiz_question.user.try(:name_or_login)      %></td></tr>
            </tbody>
        </table>
    </div>

    <!-- Display related questions -->
    <div class="related-quiz-questions">
        <% quiz_question.related_questions.each do |qq| %>
            <div class="item">
                <div class="related-q">
                    <span class="difficulty"><%= qq.perc_correct %>: </span>
                    <span  class="mcq-question"><%= qq.mc_question %></span>
                </div>
            </div>
        <% end %>
    </div>

    <!-- Display supporting references -->
    <div class="supporting-ref-display">
        <% quiz_question.supporting_verses.each do |vs| %>
            <div class="item">
                <div class="ref-and-details">
                    <span class="ref"><%= vs.translation %>: </span>
                    <span  class="first-words"><%= vs.text %></span>
                </div>
            </div>
        <% end %>
    </div>

    <br />

    <div class="quiz-question-navigation">

        <span class="approve">
            <%= link_to 'Approve', quiz_question_path(quiz_question.id, :quiz_question => {:approval_status => 'Approved'}),
                { :method => :put, :remote => true, "data-type" => "json" } %>
        </span> |

        <span class="reject ">
            <%= link_to 'Spelling',
                quiz_question_path(quiz_question.id, :quiz_question => {:approval_status => 'Rejected', :rejection_code => 'Spelling'}),
                { :method => :put, :remote => true, "data-type" => "json" } %>
        </span> |

        <span class="reject ">
            <%= link_to 'Grammar',
                quiz_question_path(quiz_question.id, :quiz_question => {:approval_status => 'Rejected', :rejection_code => 'Grammar'}),
                { :method => :put, :remote => true, "data-type" => "json" } %>
        </span> |

        <span class="reject ">
            <%= link_to 'No Answer',
                quiz_question_path(quiz_question.id, :quiz_question => {:approval_status => 'Rejected', :rejection_code => 'No Answer'}),
                { :method => :put, :remote => true, "data-type" => "json" } %>
        </span> |

        <span class="reject ">
            <%= link_to 'No Reference',
                quiz_question_path(quiz_question.id, :quiz_question => {:approval_status => 'Rejected', :rejection_code => 'No Reference'}),
                { :method => :put, :remote => true, "data-type" => "json" } %>
        </span> |

        <span class="reject ">
            <%= link_to 'Trivia',
                quiz_question_path(quiz_question.id, :quiz_question => {:approval_status => 'Rejected', :rejection_code => 'Trivia'}),
                { :method => :put, :remote => true, "data-type" => "json" } %>
        </span> |

        <span class="reject ">
            <%= link_to 'Duplicate',
                quiz_question_path(quiz_question.id, :quiz_question => {:approval_status => 'Rejected', :rejection_code => 'Duplicate'}),
                { :method => :put, :remote => true, "data-type" => "json" } %>
        </span> |

        <span class="reject ">
            <%= link_to 'Translation Dependent',
                quiz_question_path(quiz_question.id, :quiz_question => {:approval_status => 'Rejected', :rejection_code => 'Translation Dependent'}),
                { :method => :put, :remote => true, "data-type" => "json" } %>
        </span> |

        <span class="reject ">
            <%= link_to 'Ambiguous',
                quiz_question_path(quiz_question.id, :quiz_question => {:approval_status => 'Rejected', :rejection_code => 'Ambiguous'}),
                { :method => :put, :remote => true, "data-type" => "json" } %>
        </span> |

        <span class="reject ">
            <%= link_to 'Implausible Answer',
                quiz_question_path(quiz_question.id, :quiz_question => {:approval_status => 'Rejected', :rejection_code => 'Implausible Answer'}),
                { :method => :put, :remote => true, "data-type" => "json" } %>
        </span> |

        <span class="reject ">
            <%= link_to 'Other',
                quiz_question_path(quiz_question.id, :quiz_question => {:approval_status => 'Rejected', :rejection_code => 'Other'}),
                { :method => :put, :remote => true, "data-type" => "json" } %>
        </span>

    </div>

</div>
