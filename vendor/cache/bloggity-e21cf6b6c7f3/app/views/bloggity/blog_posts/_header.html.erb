<%# 
# -params-
-%>

<div id="header">
	<% if @blog %>
		<h1><%= link_to(@blog.title, blog_named_link(nil, :index, { :blog => @blog })) -%></h1>
		<p class="subtitle"><%= @blog.subtitle -%></p>
	<% else %>
		<h1>Memverse : Blog</h1> 
	<% end %>
</div>
