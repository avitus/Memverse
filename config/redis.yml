defaults: &defaults
  host: <%= ENV['WERCKER_REDIS_HOST'] %>
  port: 6379

development:
  <<: *defaults

test:
  host: <%= ENV['WERCKER_REDIS_HOST'] %>
  port: 6379

staging:
  <<: *defaults

production:
  <<: *defaults